# Sugarrush

Dead simple hyperparameter iterator and logger. You give it configs and reports, it logs how your training is going and allows you to compare between runs at the end. See `sugarrush-example.py` to see how easy it is to use.